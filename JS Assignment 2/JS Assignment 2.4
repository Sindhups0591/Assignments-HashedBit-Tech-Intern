4. Write a function to perform this. You are given two numbers n1 and n2. You need to find the sum of the products of their corresponding digits. So, 
for a number n1= 6 and n2 = 34, you'll do (6*4)+(0*3) = 24.
----------------------------------------------------------------------------------------------------------------------------------------------------

function sumOfProducts(n1, n2) {
    let str1 = n1.toString().split('').reverse();
    let str2 = n2.toString().split('').reverse();
    
    let minLength = Math.min(str1.length, str2.length);
    let sum = 0;
    
    for (let i = 0; i < minLength; i++) {
        sum += parseInt(str1[i]) * parseInt(str2[i]);
    }
    
    return sum;
}


console.log(sumOfProducts(6, 34));    // Output: 24
console.log(sumOfProducts(123, 456)); // Output: (3*6) + (2*5) + (1*4) = 38
console.log(sumOfProducts(98, 76));   // Output: (8*6) + (9*7) = 110
console.log(sumOfProducts(23, 5));    // Output: (3*5) = 15
